# VS Code workspace notes

`mcp.json.example` uses the current VS Code MCP `servers` schema with an HTTP transport. It is intentionally an `.example` file, has no credentials, and starts no local command. Only copy it to `.vscode/mcp.json` during a facilitator-guided demo when a trusted read-only server is already running on localhost.
