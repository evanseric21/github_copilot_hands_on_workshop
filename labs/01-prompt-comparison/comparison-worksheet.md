# Lab 1 — comparison worksheet

Fill this in as you go. It is a local scratch file — nobody reviews it, and you never have to commit it.
The only row that really matters is the last one: the prompt you will reuse in Lab 5.

---

## Pair 1 — `make a function`

- What Copilot had to **guess** with the weak prompt:
- The constraint I added that removed that guess:
- One concrete difference in the output:

## Pair 2 — `fix this`

- What Copilot had to **guess** with the weak prompt:
- The constraint I added that removed that guess:
- One concrete difference in the output:

## Pair 3 — `write tests`

- What Copilot had to **guess** with the weak prompt:
- The constraint I added that removed that guess:
- One concrete difference in the output:

---

## My best pair (be ready to read this one aloud)

**Weak version:**

```text

```

**Improved version:**

```text

```

**Why it worked, in one sentence:**

---

## Prompt I am keeping for Lab 5

Paste your strongest prompt here. In Lab 5 you will build the analyzer for real, under a frozen behaviour
contract, and a prompt that already names the signature, the tokenizing rule and the ordering rule will save
you several minutes.

```text

```
